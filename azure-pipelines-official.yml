# TEST ONLY (do not merge): BigBuild warm-clone cache demo, authored to EXTEND the 1ES Official
# pipeline template so it satisfies dnceng's 1ES drift-management enforcement (plain non-extends
# pipelines are policy-blocked org-wide). Same 7-step capture/replay flow; the two artifact
# publishes move into templateContext.outputs (the 1ES way) instead of PublishPipelineArtifact@1.
#
# Iterated on branch chsienki/version-stamp-warmcache-1es, queued on dnceng def 327.

trigger: none
pr: none

resources:
  repositories:
  - repository: 1ESPipelineTemplates
    type: git
    name: 1ESPipelineTemplates/1ESPipelineTemplates
    ref: refs/tags/release

parameters:
- name: slnf
  type: string
  default: 'Compilers.slnf'
- name: sdkFingerprint
  type: string
  default: 'sdk-11.0.100-preview.6-stock'

variables:
  - name: CacheStore
    value: $(Agent.TempDirectory)/wc-store
  - name: Scripts
    value: $(Build.SourcesDirectory)/eng/bigbuild-cache/scripts
  - name: Codeql.Enabled
    value: false
  - name: Codeql.SkipTaskAutoInjection
    value: true

extends:
  template: v1/1ES.Official.PipelineTemplate.yml@1ESPipelineTemplates
  parameters:
    sdl:
      sourceAnalysisPool:
        name: NetCore1ESPool-Internal
        image: 1es-windows-2022
        os: windows
    pool:
      name: NetCore1ESPool-Internal
      image: windows.vs2026.amd64
      os: windows
    stages:
    - stage: warmcache
      jobs:
      - job: warmcache
        displayName: Warm-clone cache demo (1ES)
        timeoutInMinutes: 240
        templateContext:
          outputs:
          - output: pipelineArtifact
            displayName: Publish warmcache (for next run)
            condition: succeededOrFailed()
            targetPath: $(CacheStore)
            artifactName: warmcache
          - output: pipelineArtifact
            displayName: Publish binlogs
            condition: succeededOrFailed()
            targetPath: $(Build.SourcesDirectory)/artifacts/log
            artifactName: binlogs
        steps:
        - checkout: self
          path: s
          clean: true
          fetchDepth: 0

        - task: PowerShell@2
          displayName: Restore (bootstrap SDK + packages)
          inputs:
            filePath: eng/build.ps1
            arguments: -ci -restore -prepareMachine -solution ${{ parameters.slnf }} -binaryLogName Restore.binlog

        - task: DownloadPipelineArtifact@2
          displayName: Pull warmcache (this branch)
          continueOnError: true
          inputs:
            buildType: specific
            project: $(System.TeamProjectId)
            definition: $(System.DefinitionId)
            buildVersionToDownload: latestFromBranch
            allowPartiallySucceededBuilds: true
            branchName: $(Build.SourceBranch)
            artifactName: warmcache
            targetPath: $(CacheStore)

        - task: PowerShell@2
          displayName: Check for branch cache (else fall back to main)
          inputs:
            targetType: inline
            pwsh: true
            script: |
              $laneDir = Join-Path "$(CacheStore)" "roslyn-${{ parameters.sdkFingerprint }}-win"
              $have = (Test-Path $laneDir) -and @(Get-ChildItem -Directory $laneDir -ErrorAction SilentlyContinue |
                        Where-Object { Test-Path (Join-Path $_.FullName 'meta.json') }).Count -gt 0
              Write-Host "branch cache present: $have"
              Write-Host "##vso[task.setvariable variable=HasBranchCache]$($have.ToString().ToLower())"

        - task: DownloadPipelineArtifact@2
          displayName: Pull warmcache (fall back to main)
          continueOnError: true
          condition: and(succeeded(), eq(variables['HasBranchCache'], 'false'))
          inputs:
            buildType: specific
            project: $(System.TeamProjectId)
            definition: $(System.DefinitionId)
            buildVersionToDownload: latestFromBranch
            allowPartiallySucceededBuilds: true
            branchName: refs/heads/main
            artifactName: warmcache
            targetPath: $(CacheStore)

        - task: PowerShell@2
          displayName: Seed from baseline (overlay + back-date)
          inputs:
            targetType: inline
            pwsh: true
            script: |
              $ErrorActionPreference = 'Stop'
              $laneDir = Join-Path "$(CacheStore)" "roslyn-${{ parameters.sdkFingerprint }}-win"
              $baseline = if (Test-Path $laneDir) {
                Get-ChildItem -Directory $laneDir -ErrorAction SilentlyContinue |
                  Where-Object { Test-Path (Join-Path $_.FullName 'meta.json') } |
                  Sort-Object LastWriteTime | Select-Object -Last 1
              }
              if (-not $baseline) {
                Write-Host "##[warning]No baseline (branch or main) -- COLD build (this run will seed one)."
                Write-Host "##vso[task.setvariable variable=Seeded]false"
                exit 0
              }
              $sel = $baseline.FullName
              Write-Host "==> seeding from $sel"
              & "$(Scripts)/Restore-Baseline.ps1" -RepoRoot . -BaselineDir $sel
              & "$(Scripts)/Replay-Baseline.ps1"  -RepoRoot . -BaselineDir $sel -Mode BackDate
              Write-Host "##vso[task.setvariable variable=SeededBaselineDir]$sel"
              Write-Host "##vso[task.setvariable variable=Seeded]true"

        - task: PowerShell@2
          displayName: Build (measure cold vs warm)
          inputs:
            targetType: inline
            pwsh: true
            script: |
              $ErrorActionPreference = 'Stop'
              Remove-Item -Recurse -Force "$(Build.SourcesDirectory)/artifacts/log" -ErrorAction SilentlyContinue
              $env:DOTNET_MULTILEVEL_LOOKUP = "0"
              $env:ROSLYNUSEHARDLINKS = "1"
              $dotnet = "$(Build.SourcesDirectory)\.dotnet\dotnet.exe"
              $gp = & $dotnet nuget locals global-packages --list 2>$null
              if ("$gp" -match 'global-packages:\s*(.+?)\s*$') { $env:NUGET_PACKAGES = $matches[1].Trim() }
              Write-Host "using NUGET_PACKAGES=$env:NUGET_PACKAGES"
              $sw = [Diagnostics.Stopwatch]::StartNew()
              & $dotnet build ${{ parameters.slnf }} -c Debug -p:ContinuousIntegrationBuild=true -p:DeterministicSourcePaths=true -p:EnableSourceLink=false --no-restore -bl:"$(Build.SourcesDirectory)/artifacts/log/Build.WarmCache.binlog"
              if ($LASTEXITCODE -ne 0) { throw "build failed ($LASTEXITCODE)" }
              $sw.Stop()
              Write-Host ("##[section]Build finished in {0:n1}s (seeded=$(Seeded))" -f $sw.Elapsed.TotalSeconds)

        - task: PowerShell@2
          displayName: Capture fresh baseline
          inputs:
            targetType: inline
            pwsh: true
            script: |
              $ErrorActionPreference = 'Stop'
              $store = "$(CacheStore)"
              $lane  = "roslyn-${{ parameters.sdkFingerprint }}-win"
              $sha   = (git rev-parse HEAD).Trim()
              $baselineDir = Join-Path (Join-Path $store $lane) $sha
              $capArgs = @{ RepoRoot = '.'; BaselineDir = $baselineDir; OutputRoots = @('artifacts\obj'); Archive = $true }
              $seeded = $env:SEEDEDBASELINEDIR
              if ($seeded -and (Test-Path (Join-Path $seeded 'outputs.clixml'))) {
                $stash = Join-Path "$(Agent.TempDirectory)" 'prior-baseline'
                New-Item -ItemType Directory -Force $stash | Out-Null
                Copy-Item (Join-Path $seeded 'outputs.clixml') (Join-Path $stash 'outputs.clixml') -Force
                $capArgs['PriorBaselineDir'] = $stash
              }
              if (Test-Path $store) { Remove-Item $store -Recurse -Force }
              & "$(Scripts)/Capture-Baseline.ps1" @capArgs
              Write-Host "==> captured baseline -> $baselineDir"
