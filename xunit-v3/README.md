# xUnit v3 migration

This folder holds audit artifacts for the xUnit v3 migration.
See [issue 84682](https://github.com/dotnet/roslyn/issues/84682) for details.

`filter-skip-conditions-diffs.js` runs on a diff file to make it easier to review.

`find-tests.sh` generates `out.md` and `summary.md`.

