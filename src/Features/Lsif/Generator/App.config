<?xml version="1.0" encoding="utf-8" ?>
<configuration>
  <!-- Use server GC for better performance: we care about throughput rather than responsiveness. -->
  <gcServer enabled="true" />
</configuration>